// eslint-disable-next-line no-unused-vars
import React from 'react';
import { Box } from '@mui/material';

const Results = () => {
  return (
    <Box sx={{ border: '1px solid black', height: '100%', minHeight: '400px' }}>
      Results
    </Box>
  );
};

export default Results;